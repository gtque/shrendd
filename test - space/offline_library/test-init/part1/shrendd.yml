shrendd:
  config:
    path: ./deploy/config
  test:
    local: ../../main
  targets:
    - name: render
  library:
    cache:
      dir: "~/.shrendd/cache - space"
    offline_library_lib:
      get:
        src: $_MODULE_DIR/test-init/part1/lib
        method: devD
      version: latest
      type: yaml
