shrendd:
  test:
    local: ../../main
  targets:
    - name: crib
  plugins:
    crib:
      version: "latest"
      get:
        src: "https://github.com/gtque/shrendd-lib-test/releases/latest/download/${GET_ARTIFACT}"
        method: curlD
