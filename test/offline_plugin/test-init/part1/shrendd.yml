shrendd:
  test:
    local: ../../main
  targets:
    - name: crib
  plugins:
    crib:
      version: "1.0.0"
      get:
        src: "https://github.com/gtque/shrendd-lib-test/releases/download/v1.0.0/${GET_ARTIFACT}"
        method: curlD
