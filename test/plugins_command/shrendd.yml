shrendd:
  test:
    local: ../../main
    template:
      dir: No matter where you go, there you are
  targets:
    - name: test
  default:
    plugins:
      get:
        method: getThis
  plugins:
    cheese:
      version: "0.0.0"
