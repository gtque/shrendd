test:
  hello: |-
    required: true
    description: hello world value
    default: cheeseburger
    sensitive: true
  howdy: |-
    required: false
    description: howdy world value
    default: frickle frackle
  multipass: |-
    required: false
    description: mask over multiple lines?
    default: |-
      some sensitive multi-line text 123-peek-a-boo
          that should be masked 123-peek-a-boo
          and not visible on the screen. 123-peek-a-boo
    sensitive: true
  corbin: |-
    required: false
    description: mask over multiple lines?
    default: |-
      some multi-line text
          that should not be masked
          and should be visible on the screen.
    sensitive: false
  multipass2: |-
    required: false
    description: mask over multiple lines?
    default: |-
      some additional sensitive multi-line text
          that should be masked
          and not visible on the screen.
    sensitive: true
  iambecomenothing: |-
    required: false
    description: skip masking on empty string
    default: ""
    sensitive: true
  array: |-
    required: false
    description: mask over multiple lines?
    default: |-
      - name: a 123-peek-a-boo
            description: look at me, I'm in an array!!! 123-peek-a-boo
          - name: b 123-peek-a-boo
          - name: c 123-peek-a-boo
    sensitive: true
  iambecomenothing2: |-
    required: false
    description: skip masking on empty string
    default: $(shrenddNull)
    sensitive: false
  iambecomenothing3: |-
    required: false
    description: skip masking on empty string
    default: ""
    sensitive: false
bbq:
  sauce: |-
    required: false
    suppressWarning: true
    neverSpawn: true
    description: "sweet georgia brown"
    sensitive: false #whether the field should be considered sensitive or not, any matching values will be masked in the output.
    default: "sweet georgia brown"
  rub: |-
    required: false
    suppressWarning: true
    neverSpawn: true
    description: "sweet georgia brown"
    sensitive: false #whether the field should be considered sensitive or not, any matching values will be masked in the output.
  grill: |-
    required: true
    suppressWarning: true
    neverSpawn: true
    description: "sweet georgia brown"
    sensitive: true #whether the field should be considered sensitive or not, any matching values will be masked in the output.
    default: "grill on"
    allowed: "^(|grill on|grill off)$"
  smoker: |-
    required: true
    suppressWarning: true
    neverSpawn: true
    description: "sweet georgia brown"
    sensitive: false #whether the field should be considered sensitive or not, any matching values will be masked in the output.
    default: "hickory"
    allowed: "^([a-zA-Z]+)$"
  grill2: |-
    required: true
    suppressWarning: true
    neverSpawn: true
    description: "sweet georgia brown"
    sensitive: true #whether the field should be considered sensitive or not, any matching values will be masked in the output.
    default: "grill on"
    allowed: "^(|grill on|grill off)$"
  smoker2: |-
    required: true
    suppressWarning: true
    neverSpawn: true
    description: "sweet georgia brown"
    sensitive: false #whether the field should be considered sensitive or not, any matching values will be masked in the output.
    default: "hickory"
    allowed: "^([a-zA-Z]+)$"