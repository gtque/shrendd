shrendd:
  test:
    property: "howdy, partner!"
  targets:
    - name: test
  default:
    template:
      dir: $_MODULE_DIR/deploy/${target}/abstracts
    render:
      dir: $_MODULE_DIR/deploy/${target}/generated
